name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.10'

      - name: Install dependencies
        run: bun install

      - name: Typecheck
        run: bun run typecheck

      - name: Build web app
        run: bun run build:web

      - name: Copy web assets to edge
        run: rm -rf edge/public && cp -r web/dist edge/public

      - name: Strip container config for CI deploy
        working-directory: edge
        run: node -e "const f=require('fs');const c=JSON.parse(f.readFileSync('wrangler.jsonc','utf8').replace(/\/\/.*/g,''));delete c.containers;f.writeFileSync('wrangler.jsonc',JSON.stringify(c,null,2))"

      - name: Deploy to Cloudflare
        working-directory: edge
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
